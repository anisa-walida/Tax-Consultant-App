import 'package:flutter/material.dart';
class ConfirmClient extends StatelessWidget {
  final String userUid;
   ConfirmClient({required this.userUid});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
